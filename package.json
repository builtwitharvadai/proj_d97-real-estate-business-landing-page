{
  "name": "real-estate-landing-page",
  "version": "1.0.0",
  "description": "Professional real estate business landing page with optimized performance",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx serve .",
    "build": "npm run build:css && npm run build:js && npm run build:images",
    "build:css": "cleancss -o css/styles.min.css css/styles.css",
    "build:js": "terser js/main.js -o js/main.min.js --compress --mangle && terser js/form-validator.js -o js/form-validator.min.js --compress --mangle && terser js/lazy-loading.js -o js/lazy-loading.min.js --compress --mangle",
    "build:images": "imagemin assets/images/*.webp --out-dir=assets/images/optimized --plugin=webp",
    "test:performance": "echo 'Run Lighthouse audit manually'",
    "lint:images": "echo 'Verify image optimization'"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "imagemin": "^8.0.1",
    "imagemin-cli": "^7.0.0",
    "imagemin-webp": "^8.0.0",
    "terser": "^5.31.0"
  },
  "keywords": [
    "real-estate",
    "landing-page",
    "performance",
    "optimization"
  ],
  "author": "",
  "license": "ISC"
}